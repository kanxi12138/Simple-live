# 简直播 7.0.8：刷新图标动画

日期：2026-09-20。版本：7.0.8 / 7000008。包名保持 com.simplelive.nativeapp。

- 全部三个刷新图标入口统一使用 RefreshButton：首页刷新列表、关注页刷新主播状态、播放器重新加载直播（横竖屏共用）。
- 刷新进行时图标按 900 毫秒一圈持续匀速旋转，结束后恢复静止；刷新期间禁用重复点击。动画跟随真实请求状态，不使用固定延迟模拟刷新。
- 首页跟随分类/列表 loading 状态；播放器跟随房间详情及播放源重新获取的 loading 状态，获取播放源结束即停，不等待视频第一帧，也不在正常视频缓冲时旋转。
- 关注刷新增加 ViewModel StateFlow，整批任务结束才停止，成功、部分失败和取消均通过 finally 复位；切换关注筛选页不会丢失刷新状态。
- 图标状态描述包含“刷新中”；离开页面销毁动画，应用进入后台停止旋转，回前台按当前请求状态恢复。
- 未修改文字“重试”“重连弹幕”入口及其协议，也未改变播放线路。

代码审查覆盖全部刷新图标调用点、失败/取消、空关注、重复点击及页面生命周期。release 编译成功，未运行测试套件、lint、独立类型检查或手机交互/视觉验收。

已核对 APK 包名、版本、签名与原证书一致，使用 ADB 向 A4UF6R6206008108 覆盖安装返回 Success，安装后版本确认为 7.0.8 / 7000008。保留数据和旧应用 www.sp.com，未自动启动应用、提交、推送或发布。

- [APK](Simple-live-v7.0.8-native-release.apk)
- [构建日志](artifacts/native-7.0.8-build.log)
- [包身份及 SHA-256](artifacts/native-7.0.8-package-check.txt)
- [安装核对](artifacts/native-7.0.8-install.txt)

动画实际观感未经实机验收。
