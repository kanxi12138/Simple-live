# 简直播 7.0.7：关注页左右滑动切换

日期：2026-09-20。包名：`com.simplelive.nativeapp`，版本：`7.0.7 / 7000007`。

## 交互与实现

- 关注页使用 Compose HorizontalPager，页面顺序为：正在直播、全部平台、斗鱼、虎牙、抖音、哔哩哔哩，首次默认全部平台。
- 下方列表左滑到下一页、右滑到上一页，首尾不循环。点击顶部标签动画切页，选中标签同步滚入可见区域；快速连续点击会取消上一项切页动画。
- 正在直播是独立页面，显示所有平台的开播关注；各平台页显示该平台全部关注，不再叠加开播开关。
- 每页 LazyListState 在 Pager 外独立保存，页面被懒加载回收再返回时仍保留纵向滚动位置。列表铺满剩余空间，空白处仍可翻页；无匹配关注时显示明确空状态。
- 原有分组、展开收起、刷新、置顶和管理入口保留。管理操作记录打开面板时的来源页，移动只选择该页可见、同组、同置顶状态的相邻项，沿用事务校验。
- 横向翻页和纵向列表滚动使用 Compose 原生手势处理，没有新增全屏拖动拦截器。管理弹窗打开时禁用背景手动翻页。
- 未修改数据库、配置格式、播放或弹幕协议；未增加依赖。

## 审查与验证

代码审查覆盖：六页筛选映射、标签与页面状态同步、连续点击取消、首尾边界、空列表可滑动区域、独立滚动状态、分组管理及筛选排序范围。斜向手势由原生 Pager/LazyColumn 协调，未做实机手势验收。

release 必需编译打包通过，日志：`artifacts/native-7.0.7-build.log`。没有运行测试套件、lint、独立类型检查、手机交互或视觉验收。构建有既有弃用/参数命名警告，无编译错误。

## 产物与安装

- APK：[Simple-live-v7.0.7-native-release.apk](Simple-live-v7.0.7-native-release.apk)
- APK SHA-256：`04420EFEEB4AD2ECB2C85889CCDAB1011B58148DCFAB791D712B735864B7F4FA`
- aapt 核对包名及 `7.0.7 / 7000007`；apksigner 验签通过，证书与 7.0.6 相同。
- 证书 SHA-256：`00581020a3b8faaf38fbf2c6541c6e2bdca5fbeff1f2cf53e1ac19d307433b68`
- 身份核对：`artifacts/native-7.0.7-package-check.txt`
- 指定 `A4UF6R6206008108`，`adb install -r` 返回 `Success`，安装后版本核对为 7.0.7 / 7000007。
- 安装记录：`artifacts/native-7.0.7-install.txt`。未卸载或清除数据，旧应用 `www.sp.com` 仍已安装。

滑动手感和实际交互未经手机验收。没有自动启动应用，也未提交、推送或发布远程 Release。
