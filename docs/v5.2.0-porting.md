# v5.2.0 implementation record

Protocol baseline: xiaoyaocz/dart_simple_live at ba828e6783b176ea5709fcd09f0eb01dfaceeb51, simple_live_core. See ../THIRD_PARTY_NOTICES.md and licenses/dart_simple_live-GPL-3.0.txt.

## Implemented scope

- Removed the background audio control, state synchronization, Android bridge/lifecycle takeover, service registration, Media3 dependencies and dedicated permissions from saved and generated Android projects.
- Refined mobile top bar, search, follows and settings spacing, typography, touch targets and button states. Existing portrait control rows, orientation controls, 280px danmaku panel and scrolling remain.
- Mobile Douyin search trims input, routes digits to room details and keywords to live-room search. Request generations invalidate stale results on input changes, closure and platform changes.
- Douyu: runtime search device ID, response parsing and errors, live/loop detection, H5 signing flow, actual quality and CDN order. Android retains one native socket, with stale callback guards and binary frame validation; login/group/heartbeat/STT escaping retained where equivalent.
- Huya: mobile page metadata, actual rates/CDNs, TUP3 CDN token request, AntiCode and playback UA. Danmaku uses room channel identifiers, TARS registration/chat decoding and reference heartbeat.
- Douyin: runtime cookies, enter/reflow APIs and page fallback, SDK quality names and URLs, fixed-baseline WebSocket signing JavaScript in the existing Rust/QuickJS environment, real room/user identifiers, Protobuf/gzip decoding, heartbeat and raw internal_ext ACK payload.
- Bilibili: runtime buvid/user cookies, WBI signing, real room resolution and returned playback options. Replaced blocking danmaku transport with cancellable asynchronous server/token/authentication, heartbeat, zlib/Brotli and framed message handling.
- Preserved Tauri command names and existing result fields, adding optional playback options and request headers. The shared local proxy captures each stream context, forwards FLV headers, and rewrites HLS playlists, segments, map/key URIs and relative URLs. Player/proxy/danmaku generations prevent old room callbacks from rendering into a new room.

## Validation boundary

Only frontend type checking/build, Android arm64 Rust release compilation, Android release packaging, APK metadata/signature inspection and the requested adb installation/startup are performed. No unit tests, formatter, Lint or four-platform online search/playback/danmaku functional tests are run.

Original and installed release certificate SHA-256:
00581020a3b8faaf38fbf2c6541c6e2bdca5fbeff1f2cf53e1ac19d307433b68

Explicit APK pre-deletion commands were rejected by automatic approval policy (blocked by policy). The root APK and generated build APK were subsequently overwritten with the new release. The other specified output, app/app/build/outputs/apk/arm64/release/app-arm64-release.apk, does not exist. Android compilation tasks were forced during the initial packaging pass; the final pass replaced the JNI library and repackaged it.

## Final build and installation

- Frontend: vue-tsc --noEmit and Vite production build succeeded.
- Rust: cargo build --target aarch64-linux-android --release --lib --features tauri/custom-protocol succeeded; embedded production frontend assets, copied libdtv_lib.so as ordinary files into both Android projects.
- Android: assembleArm64Release succeeded; rustBuildArm64Release, lint, lintArm64Release, lintVitalArm64Release, lintVitalAnalyzeArm64Release and lintVitalReportArm64Release were excluded. Kotlin incremental compilation was disabled for the final pass to avoid C:/D: cross-drive cache errors.
- Output: Simple-live-v5.2.0-release.apk, 53,122,750 bytes.
- aapt: www.sp.com, 5.2.0/5002000, only arm64-v8a. No BackgroundPlaybackService, FOREGROUND_SERVICE or WAKE_LOCK manifest entries.
- apksigner: signature verified; SHA-256 equals the original release and previously installed APK certificate recorded above.
- adb -s A4UF6R6206008108 install -r: Success, retaining existing data. No uninstall or data-clear command was used.
- Installed package version confirmed as 5.2.0/5002000; MainActivity cold launch returned Status: ok, process ID 6158. Installation time: 2026-09-06 18:05:29 (device time).
- No online platform search, playback or danmaku functional verification is claimed.

## Follow-up playback repair (2026-09-06)

The user subsequently reported Huya/Douyin playback failures and authorized focused diagnosis on the Honor phone. The validation boundary above records the original delivery; the following checks apply only to this repair.

- Huya: desktop CDN tokens can omit `t`. Use platform `0`, as in the pinned Dart baseline, instead of rejecting the token before constructing the playback signature.
- Douyin: a room HEAD response can be HTTP 404 while still issuing a valid runtime `ttwid`. Read those cookies before treating the status as fatal. If no usable `ttwid` is present, try the room GET and then the homepage GET, with a bounded number of requests. Preserve the final failure status instead of substituting credentials or reporting success with an unusable cookie.
- Player: preserve Rust command errors returned as strings in the existing error panel.
- Frontend type checking/production build, Android arm64 Rust release compilation and Gradle release packaging succeeded. Lint and duplicate Rust Gradle tasks remained excluded; JNI libraries were copied as ordinary files to both Android projects.
- The final APK retains www.sp.com, 5.2.0/5002000, arm64-v8a and the original release certificate above. The manifest has no background playback service or dedicated foreground service/wake lock permissions.
- Final `adb install -r` succeeded on A4UF6R6206008108, retaining data. Installed version was confirmed and MainActivity cold launch returned Status: ok. Device update time: 2026-09-06 18:31:49.
- On the final installed APK, Douyin room 341619001773 rendered 1920x1440 video: playback advanced from 13.28 to 30.65 seconds, with decoded frames increasing from 792 to 1833 and real chat messages received.
- After switching from Douyin, Huya room 10188 rendered 1920x1080 video: playback advanced from 11.24 to 35.94 seconds, with decoded frames increasing from 677 to 2159 and real chat messages received.
- The root Simple-live-v5.2.0-release.apk was replaced with this final installed build. No unit tests, formatter, Lint, four-platform search regression or exhaustive quality/CDN checks were performed. These observations establish playback for the listed rooms at the time of diagnosis, not availability of every room or platform.

## Search repair (2026-09-07)

- Huya: skip individual search entries with missing/zero room IDs instead of aborting the complete result list. On the installed release, searching 123321 returned 18 valid entries including CSBOY / 123321.
- Douyin: the upstream keyword-search response was code 2483, requiring login. Preserve nonzero platform status codes/messages and HTTP failures in the existing error panel. The command accepts an optional runtime user cookie while retaining anonymous bootstrap for existing callers.
- Added an Android official-website login dialog and a mobile Douyin search login button. The website WebView has no app JavaScript bridge; Android CookieManager persists the official website session. Returning from the dialog retries the active search with fresh cookies, without embedding or logging credentials. Both saved and generated Android sources were synchronized.
- Frontend type checking/build, arm64 Rust release compilation and assembleArm64Release succeeded. Lint and duplicate Gradle Rust tasks were excluded; JNI files were copied normally. The original release certificate, www.sp.com, version 5.2.0/5002000 and arm64-v8a were verified before installation.
- `adb install -r` succeeded on A4UF6R6206008108; MainActivity cold launch returned Status: ok. Installed update time: 2026-09-07 10:53:13. Existing data was retained.
- The official Douyin login page opens on the phone. Anonymous search accurately reports 2483. After the user completed login, the installed mobile search panel successfully searched 陈赫 using the native session and displayed one upstream live-room result (三国:天下归心), with no login error. This verifies authenticated result retrieval, not that the named person was live or that every returned recommendation is an exact name match.
- Automatic approval policy rejected the explicit old-APK deletion command (blocked by policy). Packaging and root artifact replacement subsequently overwrote the generated/root APKs. No unit tests, formatter, Lint or unrelated functional checks were run.

## Account-name search and follower ordering

- Added `search_douyin_accounts`, using the official user-search endpoint `/aweme/v1/web/discover/search/` with `search_channel=aweme_user_web` and the existing runtime login cookie. Account results include UID/sec_uid, display Douyin ID, avatar, follower count, live status and separate optional room identifiers. This is an extension outside the pinned Dart live-search baseline.
- Mobile search requests accounts and the existing live/numbered-room path concurrently. It combines results by explicit account/room identity, preserves partial successes and errors, and rejects late updates from obsolete requests. Offline accounts remain visible; unknown status is explicit; playback is enabled only for confirmed live rows with room identifiers.
- The single combined list sorts actual follower counts descending, with missing counts last and stable ties. Audience and like counts are not used as follower counts. Live-search metadata adds optional identity/follower fields without changing existing command names.
- Douyin input placeholder is exactly `输入直播关键字或抖音号`. Account identity and follower/status labels are displayed in the existing mobile result rows.
- Frontend type checking/production build and arm64 Rust release compilation succeeded. The initial Rust compilation exposed a Serde fixed-array size limitation in query parameters; converting the query array to a slice resolved it.
- Old-APK pre-deletion was again rejected by automatic approval policy (`blocked by policy`); no alternate deletion mechanism was used.
- Per the latest user instruction, no mobile search/playback functional validation, unit tests, formatter or Lint are run for this revision. The user will validate name `兔娘`, Douyin ID `R05231208`, sorting and placeholder behavior.
- Android `assembleArm64Release` succeeded with the specified exclusions. The generated and root APKs were overwritten with the new build (53,155,518 bytes). APK verification confirmed www.sp.com, 5.2.0/5002000, arm64-v8a, the original release certificate and no background playback manifest residue.
- `adb install -r` succeeded on A4UF6R6206008108 without uninstalling or clearing data. The installed package reports update time 2026-09-07 16:27:35 and version 5.2.0/5002000; MainActivity cold launch returned Status: ok.
