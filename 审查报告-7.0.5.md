# 简直播 7.0.5 全面静态审查

审查日期：2026-09-19。对象：当前 Kotlin 原生源码，版本 7.0.5 / 7000005。

## 结论

发现 8 处可从代码控制流确定的逻辑问题（7 项 P2 功能问题、1 项 P3 显示问题）。未据此断言当前应用必然闪退，也未发现可直接证明会安装错误签名 APK 的路径。建议优先处理登录态覆盖和更新状态/超时问题。

本轮只审查代码与静态资源，未修改应用实现、未打包安装、未运行测试、lint、独立类型检查或手机直播/交互/视觉验收。下述触发条件是代码推导的复现步骤，不是本轮实机实测结果。

## 发现的问题

### 1. [P2] 关闭登录窗口会用访客 Cookie 覆盖已有登录态

代码：[MainActivity.kt:68](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/MainActivity.kt:68>)

触发条件：已有有效登录后再次进入官方登录窗口，在页面设置了访客 Cookie、但尚未完成登录时按返回键关闭。

原因与影响：onDismiss 不区分完成、取消或 Activity 销毁，只要 Cookie 非空就调用 credentials.save 替换原凭证。没有验证 Cookie 对应已登录用户，也没有保护已有有效会话；取消操作可能导致原账号掉线，同时提示会话已保存。

建议修复：区分完成与取消；取消时只清理 WebView，不修改凭证。完成后通过平台会话接口确认登录有效，再替换原会话。

### 2. [P2] 更新下载继承 35 秒总请求超时

代码：[Updates.kt:34](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/Updates.kt:34>)；[Network.kt:34](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/Network.kt:34>)

触发条件：APK 下载因移动网络、限速或连接建立耗时而超过 35 秒，即便仍在正常接收数据。

原因与影响：下载客户端从通用网络客户端 newBuilder，未覆盖 callTimeout(35 秒)。该总超时覆盖读取整个响应体，所以正常但较慢的下载也会被终止；重试只能从头下载。

建议修复：下载客户端单独配置总超时策略，保留连接/读超时、大小限制和取消能力。

### 3. [P2] 切换页面丢失更新状态，并可启动冲突下载

代码：[SettingsScreen.kt:33](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/SettingsScreen.kt:33>)；[Updates.kt:33](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/Updates.kt:33>)

触发条件：开始下载后切换到首页再返回设置，或下载完成后离开设置再返回。

原因与影响：busy、downloadProgress、updateFile 都只保存在 SettingsScreen 的 remember 中，页面离开组合后丢失；下载却运行在 viewModelScope，会继续写入旧状态对象。返回页面看不到已下载文件或进行中任务，并能再次启动下载。两个任务使用同一个 .part 文件，可能互相截断、删除或重命名。签名验证能阻止错误安装，但不能避免下载失败。

建议修复：将更新状态和任务归属移到 ViewModel，恢复已校验文件入口；下载执行层保证同一时刻只有一个任务操作目标文件。

### 4. [P2] 修改关键词后加载更多会混合两次搜索

代码：[LiveViewModel.kt:119](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/LiveViewModel.kt:119>)

触发条件：先搜索 A 并得到第一页，将输入框改成 B，不点搜索而直接点加载更多。

原因与影响：searchKeyword 只更改 keyword，保留 A 的 rooms 和 page；searchRooms(append=true) 随后请求 B 的第二页，并追加到 A 的结果中。编辑时旧请求也不会失效。

建议修复：区分正在编辑的关键词和已提交的查询；分页只使用已提交查询，提交新查询时清空结果并重置页码。

### 5. [P2] HTTP 错误码丢失，导致登录恢复入口不出现

代码：[Network.kt:58](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/Network.kt:58>)；[LiveViewModel.kt:162](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/LiveViewModel.kt:162>)

触发条件：房间接口直接返回 HTTP 401 或 403，而不是 HTTP 200 中的业务错误码。

原因与影响：Network.request 构造 PlatformException 时只填写 message，code 仍为 null；播放器 needsLogin 却依赖 code 是否为 -101、401 或 403。结果提示文字要求登录，但播放器不会显示对应登录按钮，其他依赖状态码的诊断也收不到真实状态。

建议修复：在网络边界保留 HTTP 状态码；上层按平台及错误类型决定登录或稍后重试，不能将所有拒绝都等同于未登录。

### 6. [P2] 关注上移/下移使用全局邻居，和分组显示顺序不一致

代码：[Screens.kt:222](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/Screens.kt:222>)

触发条件：不同分组关注记录在全局顺序中交错，或当前开启平台/直播状态筛选时移动某位主播。

原因与影响：界面按分组、筛选和置顶显示，但操作从 library.follows 全局列表选择前后邻居。例如全局 A(组一)、B(组二)、C(组一)，在组一让 C 上移会先与不可见的 B 交换，组一仍显示 A、C，用户看到操作无效。

建议修复：明确排序范围，按当前分组和置顶层级选择实际相邻项，并保持筛选期间排序规则一致。

### 7. [P2] 斗鱼细分分类的迟到回调可能覆盖当前分类面板

代码：[LiveViewModel.kt:209](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/LiveViewModel.kt:209>)；[Screens.kt:117](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/Screens.kt:117>)

触发条件：请求细分分类 A 后关闭面板，随后切换平台或重新打开其他分类面板；A 的响应较晚到达。

原因与影响：请求运行在通用 operation 中，没有可取消的分类任务或请求代次；回调直接修改 HomeScreen 的 children。只要 HomeScreen 仍在组合中，迟到结果就可能把新面板替换成 A 的斗鱼子分类，甚至把斗鱼分类交给当前其他平台加载。

建议修复：对子分类请求绑定平台、父分类和面板代次；关闭、换平台或发起新请求时取消旧请求，并过滤迟到结果。

### 8. [P3] 弹幕每帧重新分配行，存活消息会突然换行

代码：[PlayerScreen.kt:239](<D:/Claude_Code/Simple-live - android/app/app/src/main/kotlin/com/simplelive/nativeapp/PlayerScreen.kt:239>)

触发条件：多行弹幕仍在屏幕上，其中较早的一条过期或从 200 条缓存中移除。

原因与影响：每帧新建 occupied，再只对当前存活消息从第一行重新排布；此前占据上方轨道的消息一旦被过滤，其他尚未结束的消息会被分配到新行，产生垂直跳动。

建议修复：弹幕进入显示队列时分配并保存轨道，生命周期内保持轨道不变；换房或显示布局变化时统一清理。

## 已覆盖的模块

| 模块 | 审查内容 |
| --- | --- |
| 四平台适配 | 分类/搜索/房间详情映射、直播状态、画质线路选择、签名调用与凭证来源 |
| 播放器 | 请求头与重定向校验、音频焦点、暂停/前后台切换、释放、全屏与控制面板状态 |
| 弹幕 | 四平台协议入口、大小/递归限制、异常边界、重连、取消和房间代次 |
| 登录与验证 | WebView 导航限制、取消/完成路径、Cookie 保存/清理、B站验证结果绑定 |
| 本地数据 | Room/DataStore 读写、导入校验与回滚日志、导出凭证排除、分组和排序 |
| 更新 | 下载边界、版本/包名/签名校验、FileProvider 范围、安装入口与页面状态 |
| 构建及资源 | 版本字段、Manifest、三个静态分类表和离线许可资源 |

静态分类表扫描未发现相同分组内的重复分类键：B站 386 项、抖音 306 项、虎牙 424 项。APK 安装器在下载后和安装前均校验包名、递增版本和签名；FileProvider 仅暴露 updates 缓存子目录。凭证使用 Keystore 支持的 AES-GCM 存储，配置导出走白名单，没有导出登录凭证。

以上只说明所查路径已有相关处理，不构成完整安全认证或实时接口可用性保证。

## 尚待运行证据确认的风险

- 抖音连接发送 Ping，但应用层没有 Pong 截止时间或失联计时器。网络黑洞、Wi-Fi/移动网络切换时，故障发现依赖底层 socket；可能长时间停留在“已连接”。未做断网实验，不能判断具体超时时长。
- 抖音 permessage-deflate 在库中解压后才进入 4 MiB 回调检查，检查不限制库解压过程的峰值内存。消息内 gzip 有独立输出上限。这是防护范围限制，不是已复现的崩溃。
- B站官方验证码 SDK、四平台当前接口及服务器返回结构没有在本轮联网功能验证；不能仅凭源码认定全部可用。
- 之前略过的抖音 FLV/部分播放地址问题未修改，不能纳入“已解决”范围。
- 7.0.5 之前的电脑端单房间诊断证明过抖音 HTTP 101、35 条聊天解码；不能替代手机、所有房间或长期运行验证。

## 参考

- [OkHttp 4.12.0 的客户端派生及总请求超时实现](https://github.com/square/okhttp/blob/parent-4.12.0/okhttp/src/main/kotlin/okhttp3/OkHttpClient.kt)：用于核对下载超时继承。
- [Android CookieManager 文档](https://developer.android.com/reference/android/webkit/CookieManager)：用于核对 Cookie 读取/清理语义；Cookie 非空不代表用户已登录。
