# 隐私与本地数据

本说明对应本仓库整改后的代码，不代表第三方平台的隐私政策。

## 网络请求

未发现开发者自建的数据收集、直播转发或统计服务器。平台 API、图片、CDN 及弹幕连接由用户设备发起；本机回环播放服务只在设备内部工作。平台会接收必要请求参数、设备网络地址、浏览器兼容请求头及该功能所需的 Cookie/Token。

检查更新和下载安装包会连接 GitHub API、GitHub 及其发布资源服务；打开项目主页也会连接 GitHub。因此本 App 的网络通信不只包含直播平台。APK 更新下载不属于直播下载。

没有加入广告、第三方统计、用户画像或行为追踪 SDK；开发者不出售用户信息，不向广告平台提供个人数据。即时弹幕在内存中显示，不上传至开发者数据库。

## 本地保存

主题、音量、画质/线路偏好、弹幕设置与屏蔽词、关注主播、分组及分类保存在设备的 WebView/localStorage 中。用户主动导出的配置包含关注和设置，应自行保管；导入导出不包含登录 Cookie 或直播源。旧版自定义 M3U8 数据在应用启动时清理。

真实播放地址及其参数仅在当前进程内用于播放，重复解析结果最多在内存中复用 5 秒；停止、切房及退出时清理短期播放缓存，不持久化到配置。播放器使用正常临时缓冲，App 不提供缓冲导出。

## 登录凭据：保留的风险

按维护者明确要求，本次不改变登录和 Cookie 保存方式。B站 Cookie 仍可能以 `bilibili_cookie` 保存在 WebView 的 localStorage，抖音登录使用 Android WebView CookieManager，浏览器登录窗口也可能保留平台会话。它们用于对应平台搜索、房间信息和弹幕等请求，不发送给开发者服务器，不随配置迁移。

localStorage 不是系统加密凭据库，不能声称 Cookie 已安全加密存储。设备被入侵、调试访问、恶意脚本或系统备份可能暴露本地数据。可通过应用内现有退出/清除操作处理相应登录；需要彻底清理所有本地数据时，可在系统设置中清除应用数据，该操作同时删除关注和偏好。第三方登录页面和系统 WebView 自身的数据处理遵循其提供方政策。

## 日志与联系

应用自身诊断不记录请求 URL、Cookie、Token、原始响应、弹幕内容或用户标识；发布构建关闭 WebView 调试桥和 WebView 调试开关。系统和第三方播放器自身可能产生诊断，未进行运行时全量日志验证。

隐私问题可通过 [项目 Issues](https://github.com/kanxi12138/Simple-live/issues) 联系维护者；不要公开提交 Cookie、账号信息或个人数据。
