# 简直播 7.0.9：弹幕透明度与关注自动刷新

日期：2026-09-21。包名 `com.simplelive.nativeapp`，版本 `7.0.9 / 7000009`。

## 修改

- 弹幕画笔保持不透明白色及原黑色阴影，先完成文字与阴影绘制，再通过 Compose graphicsLayer 对完整弹幕层统一施加透明度，避免半透明字形内部混入下方黑影。透明度低于 1 时使用 Offscreen 合成，为 1 时使用 Auto；没有逐帧创建 Bitmap。
- 合成修饰符仅包裹弹幕 AndroidView，不改变视频、控制面板或弹幕列表。保留原设置范围、固定轨道、帧同步、布局重建及生命周期释放；仅改变透明度不触发布局重建或清空队列。低透明度仍会正常透出视频背景，不保证在深色背景上保持不透明白色亮度。
- 仅在底部导航从其他页面切入关注时调用现有 refreshFollows，刷新全部关注。刷新任务已有互斥保护，并保留已有列表、刷新旋转动画、失败处理及 finally 复位；无关注时直接跳过。
- 从直播间返回、关注内部左右切换、重复点击已选中的关注、应用返回前台均不触发自动刷新。手动刷新入口保留。

## 审查与交付

代码审查覆盖 0.2/0.5/1 透明度路径、透明度不重排轨道、横竖屏布局重建及释放；覆盖底部切入、返回、内部切页、重复点击、空关注、已有任务和失败路径。以上为代码审查，不是手机行为实测。

release 编译成功；包名、版本和 release 签名核对通过，证书与原版一致。向 A4UF6R6206008108 执行 adb install -r 返回 Success，安装后核对为 7.0.9 / 7000009。保留数据及旧应用 www.sp.com，未启动应用验收。

- [APK](Simple-live-v7.0.9-native-release.apk)
- [构建日志](artifacts/native-7.0.9-build.log)
- [包身份、证书与 APK SHA-256](artifacts/native-7.0.9-package-check.txt)
- [安装记录](artifacts/native-7.0.9-install.txt)

未运行测试套件、lint、独立类型检查或手机视觉、直播验收。实际低透明度观感与图层性能仍需使用确认。README 未修改，没有提交或推送远程仓库。
